# Database Configuration
# For SQLite (default local development)
DATABASE_URL="file:./prisma/dev.db"

# For PostgreSQL (recommended for production on Zeabur)
# DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# Session Security
# Generate a random 32+ character secret for production
# You can generate one with: openssl rand -base64 32
SESSION_SECRET="REPLACE_WITH_RANDOM_32_CHAR_SECRET"

# Application URL (optional)
# Used for generating absolute URLs and CORS if needed
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node Environment (automatically set by most platforms)
# NODE_ENV="development"
